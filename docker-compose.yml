version: '3.8'

services:
  smartliva-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartliva_ai_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    volumes:
      - ./data/flywheel:/app/data/flywheel
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
